apiVersion: v1
kind: Service
metadata:
  name: activity-processor
  namespace: eyego-activity
spec:
  selector:
    app: activity-processor
  ports:
    - port: 3003
      targetPort: 3003
  type: ClusterIP
